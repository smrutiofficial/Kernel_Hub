/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['localhost', 'res.cloudinary.com'], // Add your Cloudinary domain here
      },
};

export default nextConfig;
