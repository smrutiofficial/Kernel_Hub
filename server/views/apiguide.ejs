<!-- views/apiGuide.ejs -->
<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>API Guide</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      line-height: 1.6;
    }

    h1,
    h2 {
      color: #333;
    }

    .route-section {
      margin-bottom: 20px;
    }

    .route {
      margin-left: 20px;
    }

    code {
      background-color: #f4f4f4;
      padding: 2px 5px;
      border-radius: 3px;
    }
  </style>
</head>

<body>
  <h1>API Guide</h1>

  <div class="route-section">
    <h2>Authentication Routes (<code>/api/auth</code>)</h2>
    <div class="route"><strong>POST</strong> <code>/api/auth/register</code> - Register a new user</div>
    <div class="route"><strong>POST</strong> <code>/api/auth/login</code> - User login</div>
    <div class="route"><strong>GET</strong> <code>/api/auth/me</code> - Get logged-in user data (requires
      authentication)</div>
    <div class="route"><strong>PUT</strong> <code>/api/auth/me</code> - Update logged-in user data (requires
      authentication)</div>
  </div>

  <div class="route-section">
    <h2>Admin Authentication Routes (<code>/api/auth/admin</code>)</h2>
    <div class="route"><strong>POST</strong> <code>/api/auth/admin/register</code> - Register a new admin</div>
    <div class="route"><strong>POST</strong> <code>/api/auth/admin/login</code> - Admin login</div>
    <div class="route"><strong>PUT</strong> <code>/api/auth/admin/me</code> - Update logged-in admin data (requires
      authentication)</div>
  </div>

  <div class="route-section">
    <h2>Post Routes (<code>/api/posts</code>)</h2>
    <div class="route"><strong>GET</strong> <code>/api/posts</code> - Retrieve all posts</div>
    <div class="route"><strong>POST</strong> <code>/api/posts/newpost</code> - Create a new post with image upload</div>
    <div class="route"><strong>GET</strong> <code>/api/posts/:id</code> - Retrieve a specific post by ID</div>
    <div class="route"><strong>PUT</strong> <code>/api/posts/:id</code> - Update a post by ID</div>
    <div class="route"><strong>DELETE</strong> <code>/api/posts/:id</code> - Delete a post by ID</div>
  </div>

  <div class="route-section">
    <h2>Comment Routes (<code>/api/comments</code>)</h2>
    <div class="route"><strong>POST</strong> <code>/api/comments</code> - Create a new comment on a post (requires
      authentication)</div>
    <div class="route"><strong>GET</strong> <code>/api/comments/:postId</code> - Retrieve comments for a specific post
    </div>
    <div class="route"><strong>GET</strong> <code>/api/comments</code> - Retrieve all comments</div>
  </div>

  <div class="route-section">
    <h2>Tag Routes (<code>/api/tags</code>)</h2>
    <div class="route"><strong>POST</strong> <code>/api/tags</code> - Create a new tag</div>
    <div class="route"><strong>GET</strong> <code>/api/tags</code> - Retrieve all tags</div>
    <div class="route"><strong>PUT</strong> <code>/api/tags/:id</code> - Update a specific tag by ID</div>
    <div class="route"><strong>DELETE</strong> <code>/api/tags/:id</code> - Delete a specific tag by ID</div>
  </div>

</body>

</html>